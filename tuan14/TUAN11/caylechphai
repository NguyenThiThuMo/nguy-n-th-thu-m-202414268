#include <stdio.h>
//kieutuantu
int main()
{
    int tree[50];

    tree[1] = 1;
    tree[3] = 2;
    tree[7] = 3;
    tree[15] = 4;
    tree[31] = 5;

    printf("CAY LECH PHAI:\n");

    printf("%d ", tree[1]);
    printf("%d ", tree[3]);
    printf("%d ", tree[7]);
    printf("%d ", tree[15]);
    printf("%d ", tree[31]);

    return 0;
}
//kieumocnoi
// #include <stdio.h>
// #include <stdlib.h>

// struct Node
// {
//     int data;
//     struct Node *left;
//     struct Node *right;
// };

// struct Node* taoNode(int x)
// {
//     struct Node *p;

//     p = (struct Node*)malloc(sizeof(struct Node));

//     p->data = x;
//     p->left = NULL;
//     p->right = NULL;

//     return p;
// }

// int main()
// {
//     struct Node *n1 = taoNode(1);
//     struct Node *n2 = taoNode(2);
//     struct Node *n3 = taoNode(3);
//     struct Node *n4 = taoNode(4);
//     struct Node *n5 = taoNode(5);

//     n1->right = n2;
//     n2->right = n3;
//     n3->right = n4;
//     n4->right = n5;

//     printf("TAO CAY LECH PHAI THANH CONG");

//     return 0;
// }